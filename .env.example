# Admin WhatsApp number in international format (no +, no spaces, no @)
# Example: 21687499959 (the bot will append @s.whatsapp.net automatically)
ADMIN_NUMBER=21687499959

# Path where Baileys stores the auth session files
# On Railway: set to /app/auth_state and mount a volume at that path
AUTH_PATH=./auth_state
